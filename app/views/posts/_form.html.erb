<%= simple_form_for @post, html: { class: 'form-horizontal', role: 'form' } do |f| %>
  <%= f.input :title %>
  <%= f.input :url %>
  <%= f.input :content %>
  
  <%= f.button :submit, value: 'Отправить' %>
<% end %>
